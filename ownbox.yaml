schema: 1
name: use-computer
description: Let an agent see and drive a GNOME Wayland desktop — screenshots, mouse and keyboard, accessibility tree, OCR and audio — as an MCP server, CLI and Claude skill.
homepage: https://github.com/BogdanStamenovic/use-computer
tags: [computer-use, mcp, claude, skill, gnome, wayland, automation, ocr, whisper, python]
install:
  platforms: [linux]
  setup:
    linux: [./ownbox.sh setup]
  update:
    linux: [./ownbox.sh update]
  remove:
    linux: [./ownbox.sh remove]
command:
  linux: .venv/bin/use-computer
skill: skills/use-computer
